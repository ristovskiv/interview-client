
import { isState } from 'json-api-client/helpers/is-state';
import { module, test } from 'qunit';

module('Unit | Helper | is state');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isState([42]);
  assert.ok(result);
});

